import en from '@/utils/translations/en';
import ru from '@/utils/translations/ru';
import ua from '@/utils/translations/ua';

const resources = {
    en,
    ru,
    ua,
} as const;

export default resources;